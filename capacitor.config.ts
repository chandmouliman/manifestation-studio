import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chandmouliman.manifestation',
  appName: 'Manifestation Studio',
  webDir: 'dist'
};

export default config;
